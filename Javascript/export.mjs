export let variable = "Hello"
console.log(variable)

let country="nepal"
export default country

/* export by name : prefix by export 
we can have multiple export by name in a file
export by default : we have only one export by default on a file  */